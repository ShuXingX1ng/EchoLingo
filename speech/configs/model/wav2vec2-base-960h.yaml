# Pinned pretrained speech encoder (ADR-0011).
# revision is the exact Hugging Face commit sha, resolved via
# https://huggingface.co/api/models/facebook/wav2vec2-base-960h on 2026-07-23.
# Do not change this file to "main" or a floating tag — reproducibility depends
# on an immutable commit identity.
model_id: facebook/wav2vec2-base-960h
revision: 22aad52d435eb6dbaf354bdad9b0da84ce7d6156
license: apache-2.0
hidden_size: 768
