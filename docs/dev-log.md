# Legacy Development Log

This file used to contain the early English MVP development log. It is now intentionally compact to avoid maintaining two parallel histories.

Use these files instead:

- `DEVELOPMENT_LOG.md` for the current compressed timeline
- `PROJECT_CONTEXT.md` for current architecture and product facts
- `TASKS.md` for active backlog
- Git history for exact old diffs

Legacy early entries covered:

- Initial Next.js + TypeScript + Tailwind setup
- Landing page and `/practice` MVP
- LLM examiner and feedback API integration
- Local history implementation

The canonical development log is now `docs/DEVELOPMENT_LOG.md`.
