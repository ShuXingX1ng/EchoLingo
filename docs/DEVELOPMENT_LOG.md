# EchoLingo Development Log

Older entries were intentionally summarized to keep this file readable. Use git history for exact diffs.

## Timeline

| Date | Entry | Summary | Validation |
|------|-------|---------|------------|
| 2026-05-13 | MVP setup | Next.js app, landing page, text practice, LLM examiner/feedback, local history | Core loop manually verified |
| 2026-05-14 | Voice and platform foundations | STT, TTS, voice controls, dark mode, PWA, accessibility, stats, goals, backups, error logging | Manual checks |
| 2026-05-15 to 2026-05-20 | Product expansion | Supabase auth/sync, topic library, recording playback, admin, i18n, recommendations, mock exam | Feature checks |
| 2026-05-21 | Learning review | Shared `FeedbackPanel` / `FeedbackReview`, history review reuse, next-action links | Unit tests added |
| 2026-05-22 | Stability and CI | Desktop nav, Next 16 config cleanup, lint cleanup, CI, E2E planning | lint/typecheck/unit/build |
| 2026-05-22 to 2026-05-23 | Data and structure | Unified history save/read, session ID consistency, error-pattern backup, recommendation improvements, audio/feedback/chat UI consolidation | lint/typecheck/unit |
| 2026-05-23 | Mobile blockers and E2E | Current mobile blockers fixed, Playwright smoke suite expanded | 20 E2E tests |
| 2026-05-23 | Python backend | FastAPI backend, LLM/Azure/Supabase services, frontend `api-client`, backend CI, deployment guide | 86 backend tests, frontend quality gates |
| 2026-05-23 | Learning plan v1 | Daily learning plan added to the home page | lint/typecheck/build/unit/E2E |
| 2026-05-24 | Docs compaction | Active docs compressed and reorganized around current state and Phase H | `git diff --check` |
| 2026-05-24 | H1 review | Reviewed learning plan data sources; no code changes; findings documented | lint/typecheck/unit (86 tests) |
| 2026-05-24 | H2 task reasons | Added `reason` field to `DailyTask`; rendered in `DailyTasks.tsx` | lint/typecheck/unit (86 tests) |
| 2026-05-24 | H2a personalization authority | Moved error pattern storage to Supabase for logged-in users; `supabase-error-patterns.ts` created; `recommendations.ts` updated | lint/typecheck/unit (86 tests)/build |
| 2026-06-06 | Architecture deepening (C1–C4) | Eliminated error-pattern dual-storage schism; deduplicated learning plan data source; lifted data fetching out of `DailyTasks`/`LearningPath`; replaced 7-arg god function with `CompletionInput` object | lint/typecheck/unit (82 tests)/build |
| 2026-06-07 | Phase 1 — PTE data model skeleton | Added PTE types (`PracticeTask`, `TaskFeedback`, `PteTaskType`, `TaskTypeWeakness`) to `src/types/index.ts`; created `task-history.ts`, `supabase-task-history.ts`, `unified-task-history.ts`, `task-weakness.ts`; added `getPteRecommendations` to `recommendations.ts`; deleted shadowing route and all shadowing components/hooks; removed shadowing refs from home, stats, nav, learning-plan, DailyTasks | typecheck 0 errors, 82/82 unit tests pass |
| 2026-06-07 | Phase 2 — Read Aloud vertical slice | New `POST /api/read-aloud/stimulus` (AI passage generation), `POST /api/read-aloud/feedback` (AI oral-fluency + pronunciation feedback); new `/practice/read-aloud` page with 6-phase state machine (idle → generating → ready → recording → processing → done); 35s prep timer, 40s recording countdown, WAV conversion via Web Audio API, parallel Azure pronunciation + AI feedback calls, `saveTask` via `unified-task-history`; home page CTA updated to link to `/practice/read-aloud` | lint 0 errors, typecheck pass |
| 2026-06-07 | Phase 2 — all remaining task slices + practice hub | `src/lib/wav-encoder.ts` (shared WAV util); `src/components/TaskFeedbackDisplay.tsx` (shared feedback UI); `POST /api/pte/stimulus` + `POST /api/pte/feedback` (unified LLM routes for all task types); 6 new pages: `/practice/repeat-sentence`, `/practice/answer-short-question`, `/practice/personal-intro`, `/practice/summarize-written-text`, `/practice/write-essay`, `/practice/write-from-dictation`; `/practice/page.tsx` replaced with PTE task-type hub grid; home CTA updated to `/practice` | lint 0 errors, typecheck pass |
| 2026-06-07 | Phase 3 — Timing enforcement (min recording guard) | Added `MIN_REC_SECONDS = 5` to `read-aloud`, `repeat-sentence`, `answer-short-question`, `personal-intro`; Stop/Done button disabled for first 5s, shows countdown label "Hold on… Xs" while waiting | typecheck 0 errors, lint 0 errors |
| 2026-06-07 | Phase 3 — Task Bank wired into all 6 task pages | Added `getStimulusFromBank` / `addStimulusToBank` calls to `read-aloud`, `repeat-sentence`, `answer-short-question`, `summarize-written-text`, `write-essay`, `write-from-dictation`; personal-intro skipped (fixed prompt) | typecheck 0 errors, lint 0 errors |
| 2026-06-07 | Agent architecture design | Grilling session resolved 7 design decisions: no Router Agent (ADR 0004); Azure PA authoritative for pronunciation, Whisper for transcription only; 10–90 scoring with PTE-aligned dimensions (ADR 0003 updated); RAGAS scoped to Summarize Written Text only; LLM-as-Judge triggers on > 15-point dimension divergence; user profile keyed by Task Type not Section; learning trajectory uses gap analysis not score prediction. New files: `docs/agent-architecture.md`, `docs/agent-architecture.html`. Updated: `CONTEXT.md`, `docs/PROJECT_CONTEXT.md`, `docs/adr/0003`, `docs/adr/0004` | Docs only, no code changes |
| 2026-06-07 | Phase 3 — Practice infrastructure | Rewrote `/history` page to display `PracticeTask` records (task-type filter tabs, search, delete, export CSV/JSON, detail view with `TaskFeedbackDisplay`); rewrote `/stats` page to show task-type weakness profile (custom `WeaknessBar` rows + ranked weakness list, practice distribution bar chart, weekly activity); added `clearAllTasksLocal` to `unified-task-history.ts`; created `src/lib/task-bank.ts` (localStorage stimulus cache: `getStimulusFromBank`, `addStimulusToBank`, `getBankSize`, `clearBankForType`, `clearAllBank`) | lint 0 errors, typecheck pass, 82/82 unit tests, build pass |
| 2026-06-07 | Phase 4 — Mock exam | `src/app/mock/page.tsx` (orchestrator: intro screen → 7-task sequence → finished state, progress bar, sessionStorage handoff); `src/app/mock/summary/page.tsx` (per-task breakdown, top weaknesses, avg pronunciation, practice links); 7 mock task components in `src/components/mock/` (MockPersonalIntro, MockReadAloud, MockRepeatSentence, MockAnswerShortQuestion, MockSummarizeWrittenText, MockWriteEssay, MockWriteFromDictation); shared `CountdownRing`; strict timing (no stop button on speaking tasks, auto-submit on written tasks); mock exam CTA added to `/practice` hub | typecheck 0 errors, lint 0 errors (1 pre-existing warning in recommendations.ts) |
| 2026-06-16 | UI Visual Upgrade — Home + Practice + Nav | Installed `lucide-react`; updated design tokens (`--border`, `--border-strong`, `--shadow-soft`, `--shadow-card`) + body gradient background in `globals.css`; `DesktopNav.tsx` icon tiles (House/PenLine/BookOpen/BarChart2/Clock/Settings), gradient logo, active inset-shadow indicator; `page.tsx` (Home): glass metric card, deep-green gradient Practice card, icon-tiled Mock/Stats cards, two-column history+skill-focus bottom; `practice/page.tsx`: hero header card with decorative dot pattern, green-bordered Theme card with big icon tile, blue-tinted Targeted card, color-coded section headers (emerald/violet/blue/amber), 3-col task cards with icon tiles + Start button | lint 0 errors (pre-existing 3 ref errors unrelated), typecheck pass, build pass |
| 2026-06-16 | UI Visual Upgrade — Stats + History pages (Round 2) | `stats/page.tsx`: removed `bg-[var(--background)]` from all wrapper divs (loading/empty/main) so body gradient shows through; added page hero card (`BarChart2` emerald icon tile); section cards use explicit `border-[#dce4ee] bg-white dark:bg-slate-900` tokens; added `AlertTriangle`/`Target`/`TrendingUp` lucide icon tiles to Weakness, Learner Profile, and Recommended sections. `history/page.tsx`: removed `bg-[var(--background)]` from outer div + `TaskDetail` wrapper; replaced inline SVG search icon with lucide `Search`; hero card `border-violet-200 bg-violet-50` explicit tokens replacing `var(--border)`. | lint 0, build pass |
| 2026-06-16 | UI Visual Upgrade — Vocabulary + Settings pages (Round 3) | `vocabulary/page.tsx`: removed `bg-[var(--background)]` from outer div; hero card and `VocabularyCard` updated to canonical `border-[#dce4ee] bg-white dark:bg-slate-900` tokens; `BookOpen` icon tile now uses explicit `border-emerald-200`. `settings/page.tsx`: removed `bg-[var(--background)]` from outer div; added page hero card (`Settings2` indigo icon tile, "Preferences / Settings" label); Goals/Reminders/Voice/Account section cards updated to canonical tokens with lucide icon tiles (`Target` emerald, `Bell` amber, `Volume2` indigo, `UserCircle2` slate); Info card updated to `rounded-[22px]` + shadow. | lint 0 errors (3 pre-existing ref errors unrelated), build pass |
| 2026-06-07 | Phase 5 — Describe Image + Re-tell Lecture | Added `"describe_image"` and `"re_tell_lecture"` to `PteTaskType` + `TaskStimulus.kind`; new `DescribeImageDetails` / `ReTellLectureDetails` types; `src/lib/image-bank.ts` (5 hardcoded public-domain chart/map image URLs); new `/practice/describe-image` (25s prep, 40s record, Web Speech transcript, AI feedback); new `/practice/re-tell-lecture` (generate via `/api/pte/stimulus`, Azure TTS, play → 10s prep → 40s record, AI feedback, Task Bank caching); `/api/pte/stimulus` + `/api/pte/feedback` extended for both task types; practice hub updated with two new cards; `ALL_TASK_TYPES`, `TASK_LABELS`, `TASK_SECTION`, `PTE_TASK_LABELS` updated across history, stats, mock/summary, recommendations, task-weakness | typecheck 0 errors, lint 0 errors, 82/82 unit tests pass |
| 2026-06-07 | Phase 6 — Learner Profile + Learning Trajectory | `src/app/stats/page.tsx` rewritten: (1) `WeaknessBar` now expands to show per-dimension sub-bars when `TaskTypeWeakness.dimensions` data exists; (2) "Learner Profile" section — SVG `RadarChart` component overlaying actual vs target scores, speaking/writing tab switcher, target slider (40–95), `GapAnalysis` bars with gap-to-target labels; (3) "Score Trajectory" section — task-type pill selector, `LineChart` of weekly avg scores per task type; aggregation helpers `aggregateProfileScores`, `taskOverallScore`, weekly score map computed in `computeStats` | typecheck 0, lint 0, 82/82 unit tests pass |
| 2026-06-07 | Phase 6 — Agent Architecture pipeline | Scoring Agent: rewrote `src/app/api/pte/feedback/route.ts` — per-dimension scores (speaking: fluency/pronunciation/content; writing: grammar/vocabulary/form/content, 0–100 internal scale); primary + Judge LLM calls run in parallel. Coach Agent: `coachSuggestions` field added to feedback (targeted per-dimension tips). LLM-as-Judge: >15-point divergence on any dimension triggers retry + attaches `judgeLog`. Diagnosis Agent: `src/lib/task-weakness.ts` extended with `aggregateDimensions` (recency-weighted per-dimension rollup); `scoreFromTask` prefers actual dimension scores over heuristic. New types in `src/types/index.ts`: `SpeakingDimensionScores`, `WritingDimensionScores`, `DimensionScores`, `JudgeLog`, `DimensionWeakness`; `TaskFeedback` + `TaskTypeWeakness` extended. `TaskFeedbackDisplay.tsx` updated with `DimensionScoresBlock` and coaching panel. | typecheck 0 errors, lint 0 errors, 82/82 unit tests pass |
| 2026-06-07 | Phase 7 — Reading section (3 task types) | Added `fill_in_the_blanks_reading`, `re_order_paragraphs`, `multiple_choice_reading` to `PteTaskType`; `ReadingDimensionScores` type (vocabulary, comprehension); 3 new `TaskFeedbackDetails` subtypes; `/api/pte/stimulus` extended with JSON-returning prompts + `response_format: json_object` + 800-token limit; `/api/pte/feedback` extended with `SCORED_READING` set, reading system prompts, detail schemas, Judge prompt, score extraction, divergence check; `aggregateDimensions` + `scoreFromTask` handle reading; `ALL_TASK_TYPES` extended; all `Record<PteTaskType, string>` lookups updated (history, stats, mock/summary, recommendations, task-weakness); `TaskFeedbackDisplay` DimensionScoresBlock + DetailsBlock extended for reading; 3 new pages: `/practice/fill-in-the-blanks` (inline dropdown blanks, timer 7 min), `/practice/re-order-paragraphs` (drag-and-drop + arrow fallback, timer 3 min), `/practice/multiple-choice` (radio buttons, timer 4 min); "Reading" section added to `/practice` hub. | typecheck 0 errors, lint 0 errors (3 pre-existing warnings), 82/82 unit tests pass |
| 2026-06-07 | Phase 8 — Listening extended types (3 task types) | Added `summarize_spoken_text`, `fill_in_the_blanks_listening`, `highlight_correct_summary` to `PteTaskType`; `ListeningDimensionScores` (comprehension, accuracy); 3 new `TaskFeedbackDetails` subtypes; `DimensionScores` union extended; `SCORED_LISTENING` set + system prompts + detail schemas + Judge prompt + score extraction + divergence check added to `/api/pte/feedback`; stimulus prompts added to `/api/pte/stimulus` (JSON_TASK_TYPES for FIB-L and HCS); `ALL_TASK_TYPES`, `scoreFromTask`, `aggregateDimensions` extended in `task-weakness.ts`; all `Record<PteTaskType, string>` lookups updated (history, stats, mock/summary, recommendations); `TaskFeedbackDisplay` DimensionScoresBlock + DetailsBlock extended for listening; 3 new pages: `/practice/summarize-spoken-text` (Azure TTS + 10 min write, word counter), `/practice/fill-in-the-blanks-listening` (Azure TTS + dropdowns, 7 min), `/practice/highlight-correct-summary` (Azure TTS + 5 radio options, 5 min); 3 listening cards added to `/practice` hub. | typecheck 0 errors, lint 0 errors (3 pre-existing warnings), 82/82 unit tests pass |
| 2026-06-08 | Mock exam extension | Added `MockSummarizeSpokenText`, `MockFillInTheBlanksListening`, `MockHighlightCorrectSummary`; `/mock` now runs a 10-task sequence | typecheck pass, lint 0 errors (3 pre-existing warnings), 82/82 unit tests pass, build pass |
| 2026-06-09 | E2E smoke tests — Listening task types | Created `e2e/listening-tasks.spec.ts` (12 tests across 3 task types); added `mockPteStimulusApi`, `mockPteFeedbackApi`, `mockAudioAutoEnd` to `e2e/helpers.ts`; covers page load, full generate→play→answer→submit flow, submit guard, and error state for each page | 12/12 E2E tests pass |
| 2026-06-10 | FastAPI backend parity — PTE stimulus + feedback | Added `backend/routers/pte_stimulus.py` (`POST /api/pte/stimulus`, all 15 task types, JSON vs plain-text mode); `backend/routers/pte_feedback.py` (Scoring Agent + LLM-as-Judge pipeline, all 4 sections, divergence retry, pronunciationAssessment merge); updated `backend/models/schemas.py` (`PteStimulusRequest`, `PteStimulusResponse`, `PteFeedbackRequest`); registered both routers in `backend/main.py`; `tests/test_pte_stimulus.py` (8 tests), `tests/test_pte_feedback.py` (11 tests) | 105/105 backend tests pass (was 86) |
| 2026-06-10 | Stats page — Listening profile tab | Added `LISTENING_SCORED_TASKS` set (SST, FIB-L, HCS); `listeningProfile` (comprehension + accuracy) computed in `computeStats`; `profileTab` state extended to `"speaking" \| "writing" \| "listening"`; tab switcher shows all available sections (≥2 required); `hasDimensionData` and `activeProfile` updated; default tab falls back to listening if no speaking/writing data | typecheck 0, lint 0 errors, 82/82 unit tests pass |
| 2026-06-10 | Mock exam — Reading section (3 tasks) | Created `MockFillInTheBlanksReading.tsx`, `MockReOrderParagraphs.tsx`, `MockMultipleChoiceReading.tsx` in `src/components/mock/`; inserted as tasks 7–9 in `TASK_SEQUENCE` in `/mock/page.tsx` (after Write Essay, before Write from Dictation); mock is now a 13-task sequence covering all four PTE sections | typecheck 0, lint 0 errors, 82/82 unit tests pass |
| 2026-06-10 | Mock exam — Describe Image + Re-tell Lecture | Created `MockDescribeImage.tsx` (auto-starts 25s prep → 40s record, image from `getRandomImage`, no stop button) and `MockReTellLecture.tsx` (auto-generates text + TTS, Play Lecture → 10s prep → 40s record); both added to `TASK_SEQUENCE` in `/mock/page.tsx` after Repeat Sentence; mock is now a 15-task sequence covering all PTE task types | typecheck 0, lint 0 errors, 82/82 unit tests pass |
| 2026-06-10 | E2E — Reading task practice pages (14 tests) | Created `e2e/reading-tasks.spec.ts`; 14 tests across FIB-Reading (5), Re-order Paragraphs (4), Multiple Choice (5); covers page load, full generate→answer→submit flow, submit guard, answered-count counter, error state; no new helpers needed | 14/14 E2E tests pass (15.0s) |
| 2026-06-10 | E2E — Mock exam full flow (9 tests) | Created `e2e/mock-exam.spec.ts`; intro screen (3): heading/nav, 15-task list with all 4 sections, Start→task-1 transition; summary page (6): no-session fallback, Exam Complete heading + task count, per-task breakdown for all 15 types, weaknesses section, practice links, action buttons; session data injected via `page.addInitScript` | 9/9 E2E tests pass (10.0s) |
| 2026-06-10 | Unit test expansion + smoke test PTE rewrite | New: `src/lib/task-weakness.test.ts` (14 tests: scoreFromTask all 4 sections, heuristic fallback, aggregateDimensions, recency weighting, rankWeaknesses), `src/lib/task-bank.test.ts` (14 tests: getStimulusFromBank, deduplication, MAX_PER_TYPE eviction, cross-type isolation, clear ops), `src/lib/unified-task-history.test.ts` (14 tests: saveTask/getTasks/getTaskById/deleteTask/getTasksByType — cloud, cloud-failure, unauthenticated branches); `e2e/smoke.spec.ts` rewritten — removed all stale IELTS routes (/practice/setup, /practice/exam, /practice/shadowing, /practice?mode=part1), replaced with 14 PTE-focused P0/P1/P2 smoke tests covering home, practice hub, read-aloud, repeat-sentence, write-essay, FIB-reading, summarize-spoken-text, mock intro, dark mode, error handling | typecheck 0 errors, lint 0, 122/122 unit tests pass |
| 2026-06-10 | Smoke test locator fixes | Fixed 3 failing smoke tests: replaced `getByText("Read Aloud")` with href-based locator (strict-mode, 2 matches); corrected write-essay button name from `/generate/i` to `/get essay question/i` in 2 tests | 14/14 smoke tests pass (23.2s) |
| 2026-06-10 | UI redesign — clean learning platform aesthetic | Replaced Arial with DM Serif Display (display headings) + DM Sans (body) via `next/font/google`; updated `globals.css` CSS variables (`--font-body`, `--font-display`, surface/border/brand tokens, dark-mode palette); redesigned `DesktopNav.tsx` (emerald pill active state + bottom-border indicator, sticky header); redesigned `MobileNav.tsx` (outline SVG icons, rounded-xl active bg); rewrote `src/app/page.tsx` (stats bar, 4-up feature card grid with primary Practice CTA, staggered entrance animations, skill rubric sidebar) | typecheck 0 errors, all pages 200, dev server healthy |
| 2026-06-10 | UI token pass — practice/setup, history, stats pages | Replaced all hardcoded `bg-white`, `bg-slate-50/900/800`, `border-slate-*`, `text-slate-*` with CSS variable tokens (`var(--surface)`, `var(--border)`, `var(--brand)`, `var(--foreground)`, `var(--text-secondary)`, `var(--text-muted)`) across `src/app/practice/setup/page.tsx`, `src/app/history/page.tsx`, `src/app/stats/page.tsx`; primary CTAs and active states updated to use `var(--brand)` | typecheck 0 errors |
| 2026-06-10 | UI token pass — all 15 practice task pages | Applied `var(--surface)`, `var(--background)`, `var(--border)`, `var(--foreground)`, `var(--text-secondary)`, `var(--text-muted)` tokens to all 15 practice task pages (read-aloud, repeat-sentence, answer-short-question, personal-intro, summarize-written-text, write-essay, write-from-dictation, describe-image, re-tell-lecture, fill-in-the-blanks, re-order-paragraphs, multiple-choice, summarize-spoken-text, fill-in-the-blanks-listening, highlight-correct-summary); 401 token usages injected across 15 files; bold-card borders intentionally kept | typecheck 0 errors |
| 2026-06-11 | Design token cleanup — `--border-strong`, exam + settings tokenize | Added `--border-strong` token (light: `#0f172a`, dark: `rgba(255,255,255,0.15)`) to `globals.css`; replaced `border-slate-900 dark:border-white/15` with `border-[var(--border-strong)]` across all 16 practice pages + hub; tokenized `practice/exam/page.tsx` and `settings/page.tsx` (surfaces, borders, labels, inputs, buttons, toggles → CSS variable tokens); Playwright visual verification: 10 screenshots (5 pages × 2 modes) + computed-style probe confirmed token resolves correctly | typecheck 0 errors, lint 0 errors, visual verification PASS |
| 2026-06-13 | Phase RAG-1 — vector store + rubric seeding scaffold | Added `langchain-community>=0.3.0` + `vecs>=0.4.0` to `backend/requirements.txt`; created `backend/services/vector_store.py` (DashScope `text-embedding-v4` via `OpenAIEmbeddings` + custom `base_url`, `vecs` Supabase pgvector client, lazy `lru_cache` singletons); created `backend/services/rag.py` (`retrieve_context(task_type) -> str`, task_type filter, silent fallback on any error); created `backend/data/rubrics/` with 5 YAML rubric files (read_aloud, repeat_sentence, write_essay, summarize_written_text, fill_in_the_blanks_reading — dimensions + scoring criteria per PTE spec); created `backend/scripts/seed_rubrics.py` (reads YAMLs, embeds via DashScope, upserts to `rubric_chunks` collection); created `backend/tests/test_rag.py` (6 unit tests, `vecs` stubbed via `sys.modules` — no install required) | 6/6 new rag tests pass; 105 existing backend tests unaffected (verified via `git stash`) |
| 2026-06-13 | i18n-Fix — zh 模式 PTE 文案修复 | Added `home.reviewProgress`/`reviewProgressDesc`/`practiceHistory` to `en.json` + `zh.json`; replaced 13 IELTS-legacy zh.json strings with PTE wording; fixed 2 pre-existing `react-hooks/refs` lint errors in `MicrophoneMonitor.tsx` | lint 0 errors; JSON valid, 0 "雅思" remaining; typecheck blocked by stale `.next/types` ref to deleted debug-supabase route (pre-existing, clears on next build) |
| 2026-06-13 | Phase RAG-2 — LangGraph feedback pipeline | Created `backend/services/feedback_graph.py`: `FeedbackState` TypedDict, 6-node `StateGraph` (retrieve_context → call_primary ‖ call_judge → check_divergence → retry_primary → finalize), RAG rubric injection into system prompt, parallel fan-out/fan-in edges, `run_feedback_graph(request)->dict`; replaced `backend/routers/pte_feedback.py` body with `run_feedback_graph` call (validation kept); `langgraph>=0.2.0` added to `requirements.txt`; made `vector_store.py` `import vecs` lazy (`try/except`) so test suite can import without `vecs` installed; fixed `routers/feedback.py` to catch `ValidationError` from `SessionFeedback(**data)`; updated all 42 pre-existing failing tests (services.llm deleted → new mock targets: `services.llm_chain.llm_chain.ainvoke`, `langchain_openai.ChatOpenAI`); created `backend/tests/test_feedback_graph.py` (21 node-level + end-to-end graph tests) | `cd backend && pytest` → 132/132 pass (111 original + 21 new graph tests) |
| 2026-06-13 | Typecheck blocker cleared | Ran `npm run build` to regenerate `.next/types/`; the stale `validator.ts` import of the deleted `debug-supabase/route.js` is gone; `npm run typecheck` now exits 0. Updated `PROJECT_CONTEXT.md` quality-gate row to "typecheck pass" | lint 0, typecheck pass (exit 0), build pass |
| 2026-06-13 | Phase Study-Aids-1 — Word Lookup + Vocabulary List | Backend: `scripts/import_ecdict.py` (downloads ECDICT 1.0.28 sqlite → gitignored `backend/data/ecdict.sqlite`), `services/ecdict.py` (read-only lookup, pos/tag parse), `prompts/word_lookup/`, `routers/word_lookup.py` (`POST /api/word-lookup`, dict-first → DeepSeek fallback), `tests/test_word_lookup.py`. Frontend: `api/word-lookup` proxy, `lib/word-lookup.ts`, `components/WordLookup.tsx` (selection "译" + drag-drop card), `practice/layout.tsx` mount (excludes `/mock`), `lib/{vocabulary,supabase-vocabulary,unified-vocabulary}.ts`, `/vocabulary` page, `supabase-migration-003.sql`, `backup.ts` vocabulary field; types `WordLookupResult`/`VocabularyEntry` | 10/10 word_lookup tests pass; live curl dict-hit returns ADR schema; ECDICT import 3.4M entries; tsc 0, eslint 0 on new files |
| 2026-06-13 | Phase RAG-3 — complete rubric coverage + RAG wiring | Authored the 10 remaining rubric YAMLs in `backend/data/rubrics/` (answer_short_question, personal_intro, describe_image, re_tell_lecture, write_from_dictation, re_order_paragraphs, multiple_choice_reading, summarize_spoken_text, fill_in_the_blanks_listening, highlight_correct_summary) — full coverage of all 15 PTE task types matching `prompts/pte/feedback_tasks.yaml` keys; added `backend/scripts/setup_pgvector.sql` (one-time `create extension vector` for Supabase dashboard); documented `DASHSCOPE_API_KEY` / `DASHSCOPE_BASE_URL` / `SUPABASE_DB_URL` in `backend/.env.example`; replaced `finalize_node` `print` with structured `logging` (per-request divergence-rate metric: `task_type`, `rag_chars`, `diverged`, `retried`); created `backend/tests/test_rubrics.py` (18 tests: rubric↔task-type coverage parity, no orphans, per-file structure validation, seed-script chunk loading) | `cd backend && pytest` → 150/150 pass (132 + 18 new rubric tests); live seeding + integration smoke deferred (require `DASHSCOPE_API_KEY` + `SUPABASE_DB_URL` not yet provisioned) |
| 2026-06-13 | Phase Cleanup — IELTS legacy code removed | Backend: deleted `routers/examiner.py`, `routers/feedback.py`, `prompts/ielts/`, and 4 IELTS test files; removed `ChatMessage`/`ErrorAnnotation`/`SessionFeedback`/`ExaminerRequest`/`ExaminerResponse`/`FeedbackRequest` from `models/schemas.py`, the IELTS examiner/evaluator loading + `examiner_system()` from `services/prompt_loader.py`, and the examiner/feedback router registrations from `main.py` (title/description → PTE); trimmed examiner/feedback cases out of `test_error_handling.py`, `test_health.py`, `test_middleware_integration.py` (CORS probes repointed to `/api/pte/feedback`). Frontend: deleted `practice/setup/` (+test), `practice/exam/page.tsx`, `lib/feedback-actions.ts`, `api/examiner/route.ts`, `api/feedback/route.ts`; removed the home-page IELTS daily-plan subsystem (`lib/learning-plan.ts`, `components/DailyTasks.tsx`, `components/LearningPath.tsx`, `components/FeedbackPanel.tsx`+`FeedbackReview`+test, dead `lib/stats.ts`); `MobileNav.tsx` Practice link `/practice/setup` → `/practice` (+test); rewrote `app/page.tsx` to drop `DailyTasks`/`LearningPath`/`generateDailyTasks`/`getRecommendations` and always render the static PTE archive card; removed dead `mockExaminerApi`/`mockFeedbackApi` from `e2e/helpers.ts`. Retained the legacy history/backup island (`lib/history.ts`, `unified-history.ts`, `supabase-history.ts`, `DataMigration.tsx`, `SessionFeedback`/`ChatMessage` types) per backup-compat. | backend 123/123 pytest pass (−27 IELTS); frontend lint 0, typecheck pass (cleared stale `.next` types), build pass, 115/115 unit (11 files, −7) |
| 2026-06-13 | Decision — frontend/backend separation (ADR 0007) | Docs/decision only, no business code changed. Verified the dual-backend fork by reading code (4 Next routes call DeepSeek `chat/completions` directly + hold `LLM_API_KEY`; `tts`/`pronunciation` hold `AZURE_SPEECH_KEY`/`REGION`; only `word-lookup` is a proper FastAPI proxy; `api-client.ts` `NEXT_PUBLIC_API_BASE_URL` optional-fallback is the fork root; FastAPI already implements every endpoint). Wrote `docs/adr/0007-frontend-backend-separation.md` (FastAPI single authority; Next `api/*` business routes migrated then deleted — no permanent proxy; secrets backend-only; `NEXT_PUBLIC_API_BASE_URL` required; degradation principle = health check + friendly error + circuit-breaker; ECDICT stays in-container for now). Updated `PROJECT_CONTEXT.md` (Backend table → target arch + per-route current/end-state, API Endpoints note, Current Status, conventions) and `TASKS.md` (new active "Phase: 前后端彻底分离" with route-by-route migrate-then-delete checklist; rewrote Resume Here; demoted Study-Aids-1 to retained live-infra subsection; dropped the now-contradictory "Removing Next.js API Routes fallback" backlog item). CONTEXT.md unchanged (no terminology change). | Docs only — no tests run; decisions confirmed against code via grep/read |
| 2026-06-13 | 前后端分离 Route 1 — `POST /api/pte/stimulus` | Added `import { apiPost }` to 14 files (12 practice pages + MockReTellLecture + MockFillInTheBlanksReading); replaced all `fetch("/api/pte/stimulus",…)` with `apiPost<{text:string}>("/api/pte/stimulus", {taskType})`; deleted `src/app/api/pte/stimulus/route.ts` | lint 0 errors, typecheck pass, 115/115 unit tests pass, build pass |
| 2026-06-13 | 前后端分离 Route 2 — `POST /api/pte/feedback` | Added `import { apiPost }` to 14 new files; replaced all 28 `fetch("/api/pte/feedback",…)` calls across 14 practice pages + 14 mock components with `apiPost<TaskFeedback>(…)`; deleted `src/app/api/pte/feedback/route.ts` (removed LangGraph+Judge+DeepSeek impl + `LLM_API_KEY` read) | lint 0 errors, typecheck pass, 115/115 unit tests pass, build pass |
| 2026-06-13 | 前后端分离 Route 3 — `POST /api/read-aloud/stimulus` + `POST /api/read-aloud/feedback` | Added `import { apiPost }` to `practice/read-aloud/page.tsx` + `src/components/mock/MockReadAloud.tsx`; replaced all 4 `fetch("/api/read-aloud/…")` calls with `apiPost`; deleted `src/app/api/read-aloud/stimulus/route.ts` + `src/app/api/read-aloud/feedback/route.ts` (removed DeepSeek direct calls + `LLM_API_KEY` read); `/api/pronunciation` fetch not touched (Route 5) | lint 0 errors, typecheck pass, 115/115 unit tests pass, build pass |
| 2026-06-13 | 前后端分离 Route 4 — `POST /api/tts` | Added `apiPostBlob` import to 12 files (6 practice pages: repeat-sentence, re-tell-lecture, write-from-dictation, summarize-spoken-text, highlight-correct-summary, fill-in-the-blanks-listening; 6 mock components: MockRepeatSentence, MockWriteFromDictation, MockSummarizeSpokenText, MockHighlightCorrectSummary, MockFillInTheBlanksListening, MockReTellLecture); replaced all 12 `fetch("/api/tts",…).blob()` with `apiPostBlob`; also fixed 5 residual `fetch("/api/pte/stimulus",…)` in mock components to `apiPost`; deleted `src/app/api/tts/route.ts` (removed Azure Speech SDK + `AZURE_SPEECH_KEY`/`REGION` read); VoiceOutput.tsx + settings/page.tsx already used apiPostBlob — no change needed | lint 0 errors, typecheck pass, 115/115 unit tests pass, build pass |
| 2026-06-13 | 前后端分离 Route 5, 6 & 收尾 | Migrated `POST /api/pronunciation` (multipart FormData) and `POST /api/word-lookup` to FastAPI via `apiPostForm` and `apiPost`; deleted Next.js routes; removed `LLM_*` and `AZURE_SPEECH_*` from `.env.example` and `.env.local`; updated `api-client.ts` to make `NEXT_PUBLIC_API_BASE_URL` required and removed fallback branch; updated `api-client.test.ts`. | lint 0, typecheck pass, 112/112 unit tests pass, build pass |
| 2026-06-13 | Phase RAG-3 & Study-Aids-1 Live Infra | Provisioned `SUPABASE_DB_URL`, run `setup_pgvector.sql` and `supabase-migration-003.sql`, seeded rubrics (53 chunks, 1024 dims via DashScope text-embedding-v4), imported ECDICT (3.4M entries), verified RAG retrieval | manual DB setup, python script integration test pass |
| 2026-06-13 | Circuit breaker & Error boundaries | Added CircuitBreaker to `api-client.ts` for fast-fail on outages; created `practice/error.tsx` and `mock/error.tsx` for graceful UI degradation; skipped vocabulary offline fallback per user request. | 14/14 unit tests pass for api-client |
| 2026-06-15 | Architecture deepening (C2) — Read Aloud backend unification | Deleted dead `backend/routers/read_aloud.py` (both `/api/read-aloud/stimulus` and `/api/read-aloud/feedback` routes); removed `read_aloud` import + router registration from `backend/main.py`; removed `randomEndpoint: "/api/read-aloud/stimulus"` from `practice/read-aloud/page.tsx` + `MockReadAloud.tsx` — stimulus now served by `/api/pte/stimulus`; added `pron_context: str` field to `FeedbackState` TypedDict; added `process_pronunciation_node` to `feedback_graph.py` (formats `pron_assessment` dict → `pron_context` string before LLM calls); rewired graph: `retrieve_context → process_pronunciation → (call_primary ‖ call_judge) → check_divergence → …`; updated `_build_user_content` to read `pron_context` from state; added 2 new graph tests; updated `_base_state` fixture with `pron_context: ""` | 294/294 backend pytest pass; tsc 0; 124/124 frontend unit tests pass |
| 2026-06-15 | Architecture deepening (C3) — Task Type metadata registry | Created `src/lib/task-type-registry.ts` (`TASK_TYPE_REGISTRY`, `ALL_TASK_TYPES`, `getTaskTypeMetadata`, `getTaskTypesByCategory`; covers all 15 PTE task types with `displayName`, `category`, `stimulusFormat`, `TimerConfig`); created `src/data/task-types.json` (canonical list of 15 task type slugs for Python to consume); updated `src/lib/task-weakness.ts` to import `ALL_TASK_TYPES` from the registry (removed inline hardcoded array; re-export preserved for backward compat); updated `backend/routers/pte_feedback.py` + `pte_stimulus.py` to load `VALID_TASK_TYPES` from `src/data/task-types.json` via `pathlib.Path` at module init (removes both hardcoded sets) | 294/294 backend pytest pass; tsc 0; 124/124 frontend unit tests pass |
| 2026-06-13 | Phase Data-1 — Migration 004: `stimulus_exemplars` table | Created `supabase-migration-004.sql`: `stimulus_exemplars` table (`id TEXT sha256 PK`, `task_type`, `text`, `embedding vector(1024)`, generated `tsv tsvector` + GIN index, `hnsw vector_cosine_ops` index, `task_type` btree index, `features JSONB`, `difficulty`, `word_count`, `lang`, `source_url`, `license`, `status`, `reason`, `created_at`); RLS enabled with no policies (PostgREST-deny pattern — backend direct Postgres connection bypasses RLS, per ADR 0009 §2); `backend/data/` already gitignored | SQL syntax self-verified; no live DB run required |
| 2026-06-13 | Phase Data-1 — Embed + dedup + upsert | Created `backend/scripts/embed_exemplars.py`: reads `status=accept` entries from `exemplars_clean/*.jsonl`, `id = sha256(normalized_text)` (matches cleaner — idempotent across stages), embeds via `services.vector_store.embed_texts` (DashScope `text-embedding-v4`, 1024-dim), per-`task_type` near-dup drop (cosine ≥ 0.95 vs stored + in-batch), idempotent `ON CONFLICT (id) DO UPDATE` upsert into `stimulus_exemplars` over a direct service-role `psycopg2` connection (`SUPABASE_DB_URL`, bypasses RLS); generated `tsv` never written; `--force` re-embeds existing rows (self-id excluded from dedup), `--source` targets one file; prints upserted + near-dup-dropped counts; added `psycopg2-binary` to `requirements.txt`; `tests/test_embed_exemplars.py` (28 tests — embed/DB mocked, no real DashScope/Supabase) | `cd backend && pytest` → 211/211 pass (28 new + 183); lint 0 |
| 2026-06-13 | Phase Data-1 — Serving + three modes + fallback + originality guard | Created `backend/services/exemplar_store.py` (`retrieve(task_type,mode,topic?,targeting?,n)` + `get_verbatim`: `random`=`ORDER BY random()`, `targeted`=task_type+difficulty/`features @> jsonb` filter, `theme`=single-SQL hybrid dense `embedding <=> vec` + sparse `plainto_tsquery` fused with RRF `FULL OUTER JOIN`; DB via `psycopg2`+`asyncio.to_thread`, silent `[]` on failure); `services/originality.py` (4-gram word-shingle Jaccard guard, threshold 0.5); `services/stimulus_service.py` (verbatim → exemplar-grounded few-shot → empty-bank pure-AI → silent pure-AI; guard regenerates once); extended `PteStimulusRequest` (`mode/topic/targeting/verbatim`); `pte_stimulus.py` kept router-thin (delegates to service); `scripts/eval_originality.py` (offline overlap-distribution eval, not in CI); 36 tests across `test_exemplar_store.py`+`test_originality.py`+`test_stimulus_service.py` (LLM/embedding/DB all mocked) | `cd backend && pytest` → 247/247 pass (36 new + 211); ruff 0 |
| 2026-06-13 | Phase Data-1 — Scraper + Cleaning pipeline | Created `backend/scripts/scrape_exemplars/` package: `base.py` (`RawExemplar` dataclass + `SourceAdapter` ABC), `sources/wikipedia.py` (`WikipediaAdapter` — REST summary API for read_aloud leads + MediaWiki explaintext API for body sections → summarize_written_text; rate-limited 1 req/s, `EchoLingo/1.0` User-Agent), `__main__.py` (argparse entry point → `exemplars_raw/<source>.jsonl`), `cleaner.py` (markup strip, English heuristic, per-task-type length gates, SHA-256 exact dedup, optional DeepSeek accept/reject gate — auto-skips when `LLM_API_KEY` absent); `backend/scripts/clean_exemplars.py` thin CLI wrapper → `exemplars_clean/<source>.jsonl` + per-task-type metrics; 60 new tests (`test_exemplar_scraper.py` + `test_exemplar_cleaner.py`, all HTTP/LLM mocked) | `cd backend && pytest` → 183/183 pass (60 new + 123 existing); lint 0 |
| 2026-06-13 | i18n — all 15 practice task pages | Added ~180 keys to `en.json`+`zh.json` under `practiceTask.common.*` and `practiceTask.<slug>.*`; added `useTranslation`+`t()` to all 15 practice task pages; `useCallback` deps updated to include `t`; PTE task type names in h1/breadcrumb kept hardcoded (proper nouns); fixed pre-existing type error in `mock/error.tsx` (`active="mock"` → no prop) | lint 0 errors, 112/112 unit tests pass, build pass |
| 2026-06-14 | Phase Data-1 Frontend — Theme Practice + Targeted Practice + mode pass-through | New `src/lib/practice-mode.ts` (`parsePracticeModeFromUrl`, `buildStimulusExtras`); `practice-mode.test.ts` (12 tests); rewrote `src/app/practice/page.tsx` — Theme Practice section (topic text input + 6 preset chips, dynamic task-card hrefs with `?mode=theme&topic=`), Targeted Practice section (top-3 weakness cards from `deriveTaskTypeWeakness`+`rankWeaknesses`, `?mode=targeted` links); added `taskType` field to TASK_CARDS; mode/topic pass-through + cache bypass in all 13 practice pages that call `/api/pte/stimulus` (`parsePracticeModeFromUrl(window.location.search)` in each `generate` callback); read-aloud special-cased to use `/api/pte/stimulus` when `isSeeded`, `/api/read-aloud/stimulus` for random; 18 new i18n keys in `en.json`+`zh.json` (`practiceHub.theme.*`, `practiceHub.targeted.*`) | lint 0 errors, typecheck pass, 124/124 unit tests pass (+12), build pass |
| 2026-06-14 | Phase Data-2 — Live Exemplar Ingestion | Extended WikipediaAdapter for write_essay, re_tell_lecture, summarize_spoken_text; ran full pipeline (scrape 3208 raw → clean → embed 1478 accept); originality eval tested; verified serving layer fallback/generation via local integration test. | 247/247 backend tests pass, full ingestion completed successfully |
| 2026-06-14 | Phase Data-3 — Expand Ingestion (4 new task types + JijingAdapter) | Extended `WikipediaAdapter` to emit `repeat_sentence`+`write_from_dictation` (sentence splits via `_split_sentences()`), `answer_short_question` (12-frame question templates), `describe_image` (8-frame chart/graph scenario templates); added length gates for all 4 in `cleaner.py`; created `sources/jijing.py` (`JijingAdapter`: reads gitignored local JSONL; public code, private data; skip-flag, limit, robust error handling); registered `jijing` in `__main__.py`; added `dimensions=1024` to `vector_store.py` OpenAIEmbeddings call; ran `--limit 10` pipeline → scrape 2945 raw → clean → embed `Upserted=2690 near-duplicates dropped=3`; DB now 4168 rows (1478 existing + 2690 new) covering all 9 text/audio task types. | `pytest tests/test_exemplar_scraper.py` → 45/45 pass; live embed verified |
| 2026-06-14 | Phase Data-4 — JSON task type exemplar banks (live embed) | Live embed (30 Wikipedia topics, --skip-llm): Upserted=3767, near-dup dropped=6; DB now **7935 rows** (was 4168); `re_order_paragraphs` first 79 rows live; 4 other JSON types blocked by global-dedup design (need composite PK migration to enable independent rows per task type). | live embed verified; 288/288 backend tests pass |
| 2026-06-14 | Migration 005 — composite PK (id, task_type) | `supabase-migration-005.sql`: drop `id` sole PK, add `PRIMARY KEY (id, task_type)`; `cleaner.py` dedup key changed to `(sha256(text), task_type)` so same text passes for multiple task types; `embed_exemplars.py` `ON CONFLICT (id)` → `(id, task_type)`; 4 new tests. Migration applied on Supabase; re-pipeline (clean --force --skip-llm + embed): Upserted=6070, near-dup dropped=10; DB now **14,005 rows** across all 14 task types. All 5 JSON task types now have Exemplar banks. | `cd backend && pytest` → 292/292 pass; live embed verified |
| 2026-06-15 | Optimize near-dedup in `embed_exemplars.py` — O(n²) → BLAS matrix op | Replaced per-item Python list copy + `is_near_duplicate` loop in `embed_and_upsert` with: (1) one-time L2-normalised `existing_normed` matrix built per task_type; (2) pre-allocated `kept_normed` buffer; (3) `existing_normed @ vec_hat` + `kept_normed[:kept_count] @ vec_hat` BLAS calls per item. `--force` self-exclusion preserved via `id_to_row` index mask. `cosine_similarity` / `is_near_duplicate` retained (used by tests). | `cd backend && pytest` → 292/292 pass |
| 2026-06-15 | Architecture deepening — extract `useRecordingSession`, `loadStimulusText`, shared `CountdownRing` | New `src/hooks/useRecordingSession.ts` (MediaRecorder + SpeechRecognition + countdown + cleanup, `onComplete(blob,tx,startedAt)` callback); new `src/lib/stimulus-loader.ts` (`loadStimulusText({taskType,randomEndpoint})` — cache-check → API → cache-store, mode-aware); new `src/components/CountdownRing.tsx` (shared); `src/components/mock/CountdownRing.tsx` → re-export; refactored `practice/read-aloud/page.tsx`, `practice/repeat-sentence/page.tsx`, `MockReadAloud.tsx`, `MockRepeatSentence.tsx` to use all three | tsc 0 errors |
| 2026-06-15 | Architecture deepening — migrate remaining 4 audio practice pages + 4 Mock components to `useRecordingSession` + `loadStimulusText` | Refactored `practice/describe-image/page.tsx`, `practice/re-tell-lecture/page.tsx`, `practice/answer-short-question/page.tsx`, `practice/personal-intro/page.tsx`; removed inline CountdownRing, inline MediaRecorder/SpeechRecognition/timerRef recording stacks, replaced with `useRecordingSession`; `re-tell-lecture` + `answer-short-question` also use `loadStimulusText` (removing `getStimulusFromBank`/`addStimulusToBank`/`parsePracticeModeFromUrl`/`buildStimulusExtras` from page scope). Same for 4 Mock components: `MockDescribeImage.tsx`, `MockReTellLecture.tsx`, `MockAnswerShortQuestion.tsx`, `MockPersonalIntro.tsx` — all recording refs + processAudioRef stacks removed; `MockReTellLecture` + `MockAnswerShortQuestion` also use `loadStimulusText`. | tsc 0 errors |
| 2026-06-15 | Architecture deepening (C1) — migrate all 9 text-input practice pages to `loadStimulusText` | Replaced the manual 10-line `parsePracticeModeFromUrl`/`buildStimulusExtras`/`getStimulusFromBank`/`addStimulusToBank`/`apiPost` stimulus block in each `generate` callback with a single `loadStimulusText({taskType})` call across all 9 pages: `write-essay`, `summarize-written-text`, `write-from-dictation`, `re-order-paragraphs`, `fill-in-the-blanks`, `multiple-choice`, `summarize-spoken-text`, `fill-in-the-blanks-listening`, `highlight-correct-summary`; removed 3 stale imports per file; `practice-mode.ts` now only consumed by `stimulus-loader.ts` | tsc 0 errors |
| 2026-06-15 | Architecture deepening (C1 收尾) — extract `usePracticeTaskRunner` hook; migrate 3 pages + 3 Mock components | New `src/hooks/usePracticeTaskRunner.ts`: encapsulates phase SM (idle→generating→writing/ready/recording→processing→done/error), countdown timer, `loadStimulusText`, feedback API call, `saveTask`, `useRecordingSession` (audio mode), Azure pronunciation (opt-in), returns `savedTask: PracticeTask\|null`. Migrated `practice/write-essay`, `practice/summarize-written-text` (text mode) and `practice/read-aloud` (audio, `withPronunciation:true`, `prepTime:35`). Migrated `MockWriteEssay`, `MockSummarizeWrittenText`, `MockReadAloud` — `onComplete(savedTask)` fires via `useEffect` on `phase==="done"`. Boundary: 7 text pages with JSON/TTS stimulus and 3 audio pages with TTS/fixed-prompt stimulus are incompatible with the hook's `generate()` and remain unchanged. | tsc 0 errors throughout |
| 2026-06-15 | Architecture deepening (C1 完成) — `submit(overrides?)` seam + migrate 3 JSON-stimulus pages + 3 Mock components | Added `overrides?: { feedbackStimulus?, feedbackResponse? }` to `submit()` in `usePracticeTaskRunner` (hook sends overrides to feedback API and saveTask response; raw JSON stored in saveTask stimulus unchanged). Migrated `multiple-choice`, `re-order-paragraphs`, `fill-in-the-blanks` pages (hook provides phase/stimulus/feedback/generate/submit; pages keep local parsed+selection state + own timer via `phase==="writing"` watch). Migrated `MockMultipleChoiceReading`, `MockReOrderParagraphs`, `MockFillInTheBlanksReading` (same pattern; replaced direct fetch+bank calls with hook's `loadStimulusText`). Fixed 3 already-migrated files (`summarize-written-text/page.tsx`, `MockSummarizeWrittenText`, `MockWriteEssay`) where `onClick={submit}` conflicted with new optional arg signature. | tsc 0 |
| 2026-06-15 | Architecture deepening (C4) — `createSyncedStore` factory | Created `src/lib/synced-store.ts` (`LocalAdapter<T,TInput>`, `CloudAdapter<T,TInput>`, `SyncedStore<T,TInput>` interfaces + `createSyncedStore` factory); refactored `unified-task-history.ts` and `unified-vocabulary.ts` to delegate save/listAll/delete to the factory; task-specific operations (`getTaskById`, `getTasksByType`, `clearAllTasksLocal`) remain inline; external signatures unchanged | tsc 0 errors; 124/124 frontend unit tests pass |
| 2026-06-16 | UI Visual Upgrade — Stats + History + Vocabulary + Settings pages | `stats/page.tsx`: main section cards `rounded-[22px]` + `shadow-[0_10px_26px_rgba(15,23,42,.055)]`; section headings `text-xs font-bold uppercase tracking-[0.19em]`; details accordions `rounded-[22px]`+shadow; Focus areas + Recommended cards upgraded. `history/page.tsx`: added `Clock` hero header card (violet icon tile + "Practice History" label), `TaskCard` `rounded-[22px]`+shadow, inline SVG trash → `Trash2`. `vocabulary/page.tsx`: hero card with `BookOpen` emerald icon tile, `VocabularyCard` `rounded-[22px]`+shadow, SVG trash → `Trash2`. `settings/page.tsx`: all 4 main cards `rounded-[22px]`+shadow; colored section labels (Goals/Reminders/Voice/Account) above each h2; toggles active color `var(--foreground)` → `var(--brand)`. | lint 0 errors (3 pre-existing ref errors in unrelated files); build pass |
| 2026-06-17 | Phase Study-Assistant-1 — learner-facing tool-using Study Assistant (ADR-0010) | BE: `services/study_assistant_graph.py` (LangGraph tool loop, recency gate in node), `tools/study_assistant_tools.py` (navigate_app/generate_practice/pte_knowledge + route allow-list filter), `services/gnews.py` (title+summary, silent fallback), `prompts/study_assistant/knowledge.yaml`, `routers/study_assistant.py` (`POST /api/study-assistant/chat`); `source=news` plumbed through `PteStimulusRequest`/`stimulus_service.generate_stimulus` reusing originality guard. FE: `components/StudyAssistant.tsx` (global floating panel, hidden on `/mock*`, session-only chat, deep-link handoff only), `lib/study-assistant.ts`, `practice-mode.ts` source param, `en/zh.json` `studyAssistant.*`. No new Supabase table; no login. | backend pytest 343 pass; ruff clean on new files; FE tsc 0, eslint clean (touched), i18n 12/12, build pass |
| 2026-06-17 | Portfolio README refresh | Rewrote `README.md` for GitHub: PTE scope, Agent workflows, RAG/retrieval, setup, tests, Claude Code-assisted workflow | Docs-only change; not run |
## Major Completed Capabilities

- IELTS Part 1/2/3 practice with AI examiner
- Full mock exam flow
- Structured feedback and estimated band score
- Error annotations and improved sample answer
- Azure Neural TTS and Pronunciation Assessment
- Shadowing practice with per-sentence scoring
- Unified learning review UI (`FeedbackPanel` / `FeedbackReview`)
- History review and stats learning progress
- Supabase auth and cloud sync with local fallback
- Recording playback through IndexedDB
- Desktop and mobile navigation
- Admin, topics, users, i18n
- Frontend CI, backend CI, 20 E2E smoke tests
- FastAPI backend beside Next.js API Routes fallback
- Daily learning plan with Supabase-authoritative personalization

## Documentation Policy

After meaningful development work, update:

- `docs/DEVELOPMENT_LOG.md` (add a row to Timeline)
- `docs/PROJECT_CONTEXT.md` (update Current Status if needed)
- `docs/TASKS.md` (mark completed, add next phase tasks)

Keep entries short and factual: what changed, key files, validation actually run.
